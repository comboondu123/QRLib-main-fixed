services:
  qrlib:
    build: .
    image: qrlib:latest
    container_name: qrlib
    restart: unless-stopped
    ports:
      # 宿主机端口可用 PORT 覆盖，例如 PORT=8080 docker compose up -d
      - "${PORT:-3000}:3000"
    environment:
      PORT: 3000
      HOST: 0.0.0.0
      WEBUI_ENABLED: "${WEBUI_ENABLED:-true}"
      REQUEST_TIMEOUT: "${REQUEST_TIMEOUT:-15000}"
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
